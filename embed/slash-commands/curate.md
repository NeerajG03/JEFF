---
description: Run marlowe to consolidate proposals into canonical memory
---

Run: `jeff memory curate`

Stream output to the user. If conflicts are reported, ask the user how to resolve each one
before re-running with `--auto` or applying manual fixes.
